{
  "name": "ci-cd-web",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "jest --runInBand",
    "build": "cp -r src dist"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
